# Minimal Target Example

This is a placeholder target you can use to smoke-test the runner.

Copy `examples/replace_with_real_success_gate.py` into a real target repo as `scripts/replace_with_real_success_gate.py`, or replace the template config's success command with a command that matches your project.
